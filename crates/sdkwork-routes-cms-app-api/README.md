# sdkwork-routes-cms-app-api

App API route adapter for SDKWork CMS.

Prefix: `/app/v3/api/cms`

Implementation:
- Routes bound to manifest supporting anonymous published delivery.
- Preview token validation supported through `CmsPreviewTokenPort`.
- App delivery endpoints separate from backend-admin management endpoints.
